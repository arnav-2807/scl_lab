def defuzzify(x, mu):
    num = 0
    den = 0
    for i in range(len(x)):
        num += x[i] * mu[i]
        den += mu[i]
    return num / den

x = [1, 2, 3, 4]
mu = [0.1, 0.5, 0.8, 0.2]

print(defuzzify(x, mu))
